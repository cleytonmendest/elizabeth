{%- comment -%}
  Product Schema (JSON-LD) para SEO
  Melhora exibição nos resultados do Google (rich snippets)

  Parâmetros:
  - product: objeto do produto (obrigatório)
{%- endcomment -%}

{%- liquid
  assign selected_variant = product.selected_or_first_available_variant
  assign product_image = product.featured_image | image_url: width: 1200

  comment
    Calcula o rating médio (placeholder - será substituído por app de reviews)
  endcomment
  assign review_count = 0
  assign rating_value = 0
-%}

<script type="application/ld+json">
{
  "@context": "https://schema.org/",
  "@type": "Product",
  "name": {{ product.title | json }},
  "description": {{ product.description | strip_html | truncatewords: 150 | json }},
  "image": [
    {%- for image in product.images limit: 5 -%}
      {{ image | image_url: width: 1200 | json }}{%- unless forloop.last -%},{%- endunless -%}
    {%- endfor -%}
  ],
  "sku": {{ selected_variant.sku | default: selected_variant.id | json }},
  "brand": {
    "@type": "Brand",
    "name": {{ product.vendor | default: shop.name | json }}
  },
  {%- if product.metafields.custom.gtin -%}
  "gtin": {{ product.metafields.custom.gtin | json }},
  {%- endif -%}
  {%- if product.metafields.custom.mpn -%}
  "mpn": {{ product.metafields.custom.mpn | json }},
  {%- endif -%}
  "offers": {
    "@type": "Offer",
    "url": {{ request.origin | append: product.url | json }},
    "priceCurrency": {{ cart.currency.iso_code | json }},
    "price": {{ selected_variant.price | divided_by: 100.0 | json }},
    {%- if selected_variant.compare_at_price -%}
    "priceValidUntil": "{{ 'now' | date: '%s' | plus: 2592000 | date: '%Y-%m-%d' }}",
    {%- endif -%}
    "availability": "{% if selected_variant.available %}https://schema.org/InStock{% else %}https://schema.org/OutOfStock{% endif %}",
    "itemCondition": "https://schema.org/NewCondition",
    "seller": {
      "@type": "Organization",
      "name": {{ shop.name | json }}
    }
  }
  {%- comment -%}
    Rating/Reviews será adicionado automaticamente pelo app de reviews (Judge.me ou Loox)
    Quando instalar, o app injetará:
    "aggregateRating": {
      "@type": "AggregateRating",
      "ratingValue": "4.8",
      "reviewCount": "127"
    }
  {%- endcomment -%}
}
</script>
