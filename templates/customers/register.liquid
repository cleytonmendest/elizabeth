<!-- /templates/customers/register.liquid -->

<div class="color-{{ settings.customer_color_scheme }} color-background color-text min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-md w-full space-y-8">

    {%- comment -%} Header {%- endcomment -%}
    <div class="text-center">
      {%- if settings.logo -%}
        <a href="{{ routes.root_url }}" class="inline-block mb-6">
          <img
            src="{{ settings.logo | image_url: width: 120 }}"
            alt="{{ shop.name }}"
            width="120"
            height="auto"
            class="mx-auto"
            loading="eager"
          >
        </a>
      {%- endif -%}

      <h1 class="text-3xl font-bold text-foreground mb-2">
        {{ 'customer.register.heading' | t }}
      </h1>

      <p class="text-sm text-foreground/60">
        {{ 'customer.register.subtext' | t }}
      </p>
    </div>

    {%- comment -%} Registration Form {%- endcomment -%}
    <div class="bg-background shadow-lg rounded-lg p-8">

      {%- form 'create_customer', novalidate: 'novalidate', class: 'space-y-6' -%}

        {%- comment -%} Error Messages {%- endcomment -%}
        {%- if form.errors -%}
          <div class="color-error-bg border color-border rounded-lg p-4" role="alert">
            <div class="flex items-start">
              <svg class="w-5 h-5 color-error mt-0.5 mr-3 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
              </svg>
              <div class="text-sm color-error">
                <p class="font-medium mb-1">{{ 'customer.shared.form_errors_title' | t }}</p>
                {{ form.errors | default_errors }}
              </div>
            </div>
          </div>
        {%- endif -%}

        {%- comment -%} Name Fields {%- endcomment -%}
        <div class="grid grid-cols-1 gap-6 sm:grid-cols-2">
          <div>
            <label for="first_name" class="block text-sm font-medium text-foreground/70 mb-2">
              {{ 'customer.register.first_name' | t }} <span class="color-error">*</span>
            </label>
            <input
              type="text"
              name="customer[first_name]"
              id="first_name"
              autocomplete="given-name"
              required
              aria-required="true"
              class="w-full px-4 py-3 border border-border rounded-lg focus:ring-2 focus:ring-foreground focus:border-transparent transition-all outline-none"
              placeholder="{{ 'customer.register.first_name_placeholder' | t }}"
              {%- if form.first_name -%}
                value="{{ form.first_name }}"
              {%- endif -%}
            >
          </div>

          <div>
            <label for="last_name" class="block text-sm font-medium text-foreground/70 mb-2">
              {{ 'customer.register.last_name' | t }} <span class="color-error">*</span>
            </label>
            <input
              type="text"
              name="customer[last_name]"
              id="last_name"
              autocomplete="family-name"
              required
              aria-required="true"
              class="w-full px-4 py-3 border border-border rounded-lg focus:ring-2 focus:ring-foreground focus:border-transparent transition-all outline-none"
              placeholder="{{ 'customer.register.last_name_placeholder' | t }}"
              {%- if form.last_name -%}
                value="{{ form.last_name }}"
              {%- endif -%}
            >
          </div>
        </div>

        {%- comment -%} Email Field {%- endcomment -%}
        <div>
          <label for="email" class="block text-sm font-medium text-foreground/70 mb-2">
            {{ 'customer.register.email' | t }} <span class="color-error">*</span>
          </label>
          <input
            type="email"
            name="customer[email]"
            id="email"
            autocomplete="email"
            required
            aria-required="true"
            class="w-full px-4 py-3 border border-border rounded-lg focus:ring-2 focus:ring-foreground focus:border-transparent transition-all outline-none"
            placeholder="{{ 'customer.shared.email_placeholder' | t }}"
            {%- if form.email -%}
              value="{{ form.email }}"
            {%- endif -%}
          >
          <p class="mt-1 text-xs text-foreground/50">
            {{ 'customer.register.email_hint' | t }}
          </p>
        </div>

        {%- comment -%} Password Field {%- endcomment -%}
        <div>
          <label for="password" class="block text-sm font-medium text-foreground/70 mb-2">
            {{ 'customer.register.password' | t }} <span class="color-error">*</span>
          </label>
          <div class="relative">
            <input
              type="password"
              name="customer[password]"
              id="password"
              autocomplete="new-password"
              required
              aria-required="true"
              minlength="5"
              class="w-full px-4 py-3 border border-border rounded-lg focus:ring-2 focus:ring-foreground focus:border-transparent transition-all outline-none"
              placeholder="••••••••"
            >
            <button
              type="button"
              id="toggle-password"
              class="absolute inset-y-0 right-0 pr-3 flex items-center text-foreground/40 hover:text-foreground/60 transition-colors"
              aria-label="{{ 'customer.shared.toggle_password' | t }}"
            >
              <svg id="eye-icon" class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/>
              </svg>
              <svg id="eye-off-icon" class="w-5 h-5 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.875 18.825A10.05 10.05 0 0112 19c-4.478 0-8.268-2.943-9.543-7a9.97 9.97 0 011.563-3.029m5.858.908a3 3 0 114.243 4.243M9.878 9.878l4.242 4.242M9.88 9.88l-3.29-3.29m7.532 7.532l3.29 3.29M3 3l3.59 3.59m0 0A9.953 9.953 0 0112 5c4.478 0 8.268 2.943 9.543 7a10.025 10.025 0 01-4.132 5.411m0 0L21 21"/>
              </svg>
            </button>
          </div>

          {%- comment -%} Password Strength Indicator {%- endcomment -%}
          <div id="password-strength" class="mt-2 hidden">
            <div class="flex items-center space-x-2">
              <div class="flex-1 h-1.5 bg-foreground/10 rounded-full overflow-hidden">
                <div id="strength-bar" class="h-full transition-all duration-300 rounded-full"></div>
              </div>
              <span id="strength-text" class="text-xs font-medium"></span>
            </div>
            <p id="strength-hint" class="mt-1 text-xs text-foreground/50"></p>
          </div>

          <p class="mt-1 text-xs text-foreground/50">
            {{ 'customer.register.password_hint' | t }}
          </p>
        </div>

        {%- comment -%} Marketing Acceptance {%- endcomment -%}
        <div class="flex items-start">
          <div class="flex items-center h-5">
            <input
              type="checkbox"
              name="customer[accepts_marketing]"
              id="accepts_marketing"
              class="w-4 h-4 border-border rounded text-foreground focus:ring-foreground"
            >
          </div>
          <div class="ml-3">
            <label for="accepts_marketing" class="text-sm text-foreground/70">
              {{ 'customer.register.accepts_marketing' | t }}
            </label>
          </div>
        </div>

        {%- comment -%} Terms Acceptance {%- endcomment -%}
        <div class="flex items-start">
          <div class="flex items-center h-5">
            <input
              type="checkbox"
              id="terms_acceptance"
              required
              aria-required="true"
              class="w-4 h-4 border-border rounded text-foreground focus:ring-foreground"
            >
          </div>
          <div class="ml-3">
            {%- capture terms_link -%}<a href="{{ pages.termos-de-uso.url | default: '/pages/terms' }}" target="_blank" class="font-medium text-foreground hover:text-foreground/70 underline">{{ 'customer.register.terms_of_use' | t }}</a>{%- endcapture -%}
            {%- capture privacy_link -%}<a href="{{ pages.politica-de-privacidade.url | default: '/pages/privacy' }}" target="_blank" class="font-medium text-foreground hover:text-foreground/70 underline">{{ 'customer.register.privacy_policy' | t }}</a>{%- endcapture -%}
            <label for="terms_acceptance" class="text-sm text-foreground/70">
              {{ 'customer.register.terms_agreement_html' | t: terms_link: terms_link, privacy_link: privacy_link }}
              <span class="color-error">*</span>
            </label>
          </div>
        </div>

        {%- comment -%} Submit Button {%- endcomment -%}
        <button
          type="submit"
          class="w-full color-button py-3 px-4 rounded-lg font-medium hover:opacity-90 transition-colors focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-foreground disabled:opacity-50 disabled:cursor-not-allowed"
        >
          {{ 'customer.register.submit_button' | t }}
        </button>

      {%- endform -%}

      {%- comment -%} Login Link {%- endcomment -%}
      <div class="text-center pt-6 mt-6 border-t border-border">
        <p class="text-sm text-foreground/60">
          {{ 'customer.register.already_have_account' | t }}
          <a
            href="{{ routes.account_login_url }}"
            class="font-medium text-foreground hover:text-foreground/70 transition-colors ml-1"
          >
            {{ 'customer.register.login_link' | t }}
          </a>
        </p>
      </div>
    </div>

    {%- comment -%} Back to Store {%- endcomment -%}
    <div class="text-center">
      <a
        href="{{ routes.root_url }}"
        class="text-sm text-foreground/60 hover:text-foreground transition-colors inline-flex items-center"
      >
        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
        </svg>
        {{ 'customer.shared.back_to_store' | t }}
      </a>
    </div>

  </div>
</div>

{%- comment -%} Password Toggle & Validation Script {%- endcomment -%}
<script>
  (function() {
    const passwordInput = document.getElementById('password');
    const toggleButton = document.getElementById('toggle-password');
    const eyeIcon = document.getElementById('eye-icon');
    const eyeOffIcon = document.getElementById('eye-off-icon');
    const strengthContainer = document.getElementById('password-strength');
    const strengthBar = document.getElementById('strength-bar');
    const strengthText = document.getElementById('strength-text');
    const strengthHint = document.getElementById('strength-hint');
    const termsCheckbox = document.getElementById('terms_acceptance');
    const form = document.querySelector('form');
    const submitButton = form.querySelector('button[type="submit"]');

    {%- comment -%} Toggle Password Visibility {%- endcomment -%}
    if (toggleButton && passwordInput) {
      toggleButton.addEventListener('click', function() {
        const type = passwordInput.type === 'password' ? 'text' : 'password';
        passwordInput.type = type;

        if (type === 'password') {
          eyeIcon.classList.remove('hidden');
          eyeOffIcon.classList.add('hidden');
        } else {
          eyeIcon.classList.add('hidden');
          eyeOffIcon.classList.remove('hidden');
        }
      });
    }

    {%- comment -%} Password Strength Checker {%- endcomment -%}
    if (passwordInput && strengthContainer) {
      passwordInput.addEventListener('input', function() {
        const password = passwordInput.value;

        if (password.length === 0) {
          strengthContainer.classList.add('hidden');
          return;
        }

        strengthContainer.classList.remove('hidden');

        let strength = 0;
        let hints = [];

        // Length check
        if (password.length >= 8) strength++;
        else hints.push({{ 'customer.register.strength_hints.length' | t | json }});

        // Lowercase check
        if (/[a-z]/.test(password)) strength++;
        else hints.push({{ 'customer.register.strength_hints.lowercase' | t | json }});

        // Uppercase check
        if (/[A-Z]/.test(password)) strength++;
        else hints.push({{ 'customer.register.strength_hints.uppercase' | t | json }});

        // Number check
        if (/\d/.test(password)) strength++;
        else hints.push({{ 'customer.register.strength_hints.number' | t | json }});

        // Special character check
        if (/[^A-Za-z0-9]/.test(password)) strength++;
        else hints.push({{ 'customer.register.strength_hints.special' | t | json }});

        // Update strength bar and text
        const colors = ['color-error-bg0', 'bg-orange-500', 'bg-yellow-500', 'bg-green-500', 'bg-green-600'];
        const labels = [
          {{ 'customer.password_strength.very_weak' | t | json }},
          {{ 'customer.password_strength.weak' | t | json }},
          {{ 'customer.password_strength.medium' | t | json }},
          {{ 'customer.password_strength.good' | t | json }},
          {{ 'customer.password_strength.very_good' | t | json }}
        ];
        const textColors = ['color-error', 'text-orange-600', 'color-warning', 'color-success', 'color-success'];

        strengthBar.className = 'h-full transition-all duration-300 rounded-full ' + colors[strength - 1];
        strengthBar.style.width = (strength * 20) + '%';
        strengthText.className = 'text-xs font-medium ' + textColors[strength - 1];
        strengthText.textContent = labels[strength - 1];

        if (hints.length > 0 && strength < 4) {
          strengthHint.textContent = {{ 'customer.password_strength.add_prefix' | t | json }} + ' ' + hints.join(', ');
        } else {
          strengthHint.textContent = {{ 'customer.password_strength.strong' | t | json }};
        }
      });
    }

    {%- comment -%} Form Validation {%- endcomment -%}
    if (form) {
      const inputs = form.querySelectorAll('input[required]');

      inputs.forEach(function(input) {
        input.addEventListener('invalid', function(e) {
          e.preventDefault();
          input.classList.add('border-error', 'focus:ring-error');

          // Show custom error message
          const label = input.previousElementSibling;
          if (label && label.tagName === 'LABEL') {
            let errorMsg = label.querySelector('.error-message');
            if (!errorMsg) {
              errorMsg = document.createElement('span');
              errorMsg.className = 'error-message text-xs color-error ml-1';
              label.appendChild(errorMsg);
            }
            errorMsg.textContent = {{ 'customer.register.required_field_error' | t | json }};
          }
        });

        input.addEventListener('input', function() {
          if (input.validity.valid) {
            input.classList.remove('border-error', 'focus:ring-error');

            // Remove error message
            const label = input.previousElementSibling;
            if (label && label.tagName === 'LABEL') {
              const errorMsg = label.querySelector('.error-message');
              if (errorMsg) {
                errorMsg.remove();
              }
            }
          }
        });
      });

      // Terms checkbox validation
      if (termsCheckbox) {
        form.addEventListener('submit', function(e) {
          if (!termsCheckbox.checked) {
            e.preventDefault();
            termsCheckbox.focus();

            // Show error message
            const container = termsCheckbox.closest('div').parentElement;
            let errorDiv = container.querySelector('.terms-error');

            if (!errorDiv) {
              errorDiv = document.createElement('div');
              errorDiv.className = 'terms-error text-xs color-error mt-1 ml-7';
              errorDiv.textContent = {{ 'customer.register.terms_required' | t | json }};
              container.appendChild(errorDiv);
            }
          }
        });

        termsCheckbox.addEventListener('change', function() {
          if (termsCheckbox.checked) {
            const container = termsCheckbox.closest('div').parentElement;
            const errorDiv = container.querySelector('.terms-error');
            if (errorDiv) {
              errorDiv.remove();
            }
          }
        });
      }
    }
  })();
</script>

<style>
  {%- comment -%} Error list styling {%- endcomment -%}
  .errors ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .errors li {
    margin-bottom: 0.25rem;
  }

  .errors li:last-child {
    margin-bottom: 0;
  }

  {%- comment -%} Custom checkbox styling {%- endcomment -%}
  input[type="checkbox"]:checked {
    background-color: rgb(17, 24, 39);
    border-color: rgb(17, 24, 39);
  }

  input[type="checkbox"]:focus {
    outline: 2px solid transparent;
    outline-offset: 2px;
    box-shadow: 0 0 0 2px white, 0 0 0 4px rgb(17, 24, 39);
  }
</style>
