{% assign loop = section.settings.loop %}
{% assign nav = section.settings.nav %}
{% assign qty_desk = section.settings['qty-desk'] %}
{% assign qty_tab = section.settings['qty-tablet'] %}
{% assign qty_mob = section.settings['qty-mob'] %}
{% assign autoplay = section.settings.autoplay %}
{% assign play_time = section.settings['play-time'] %}
{% assign pause_hover = section.settings['pause-hover'] %}
{% assign dot = section.settings.dot %}

<style>
  my-slider.hero-slider .swiper-pagination {
    position: absolute; bottom: 12px; left: 0; right: 0;
    display: flex; justify-content: center; align-items: center; gap: 6px; z-index: 10;
  }
  my-slider.hero-slider .swiper-pagination-bullet {
    width: 6px; height: 6px; margin: 0 !important; border-radius: 9999px;
    background: rgb(var(--color-foreground)); opacity: .35; transition: all .25s ease;
  }
  my-slider.hero-slider .swiper-pagination-bullet-active { opacity: 1; width: 18px; border-radius: 3px; }
</style>

<div class="color-{{ section.settings.color_scheme }} bg-background">
  <div class="{% if section.settings.fullwidth %}w-full{% else %}page-width{% endif %}">
    <my-slider
      class="hero-slider w-full relative"
    data-section-id="{{ section.id }}"
    data-loop="{{ loop }}"
    data-items="{{ section.blocks.size }}"
    data-qty-desk="{{ qty_desk }}"
    data-qty-tab="{{ qty_tab }}"
    data-qty-mob="{{ qty_mob }}"
    data-autoplay="{{ autoplay }}"
    data-ap-time="{{ play_time }}"
    data-pause-hover="{{ pause_hover }}"
    data-dot="{{ dot }}"
  >
    {% comment %} OBRIGATORIO TER A DIV "my-slider__container" PARA O CORRETO FUNCIONAMENTO {% endcomment %}
    <div class="my-slider__container overflow-hidden">
      {%- for block in section.blocks -%}
        {%- liquid
          comment
            Otimização de performance:
            - Primeiro slide: loading="eager" fetchpriority="high" (LCP - Largest Contentful Paint)
            - Demais slides: loading="lazy" (economiza banda)
            - decoding="async" em todos (não bloqueia renderização)
          endcomment
          assign is_first_slide = false
          if forloop.first
            assign is_first_slide = true
          endif

          assign content_position = block.settings.content_position | default: 'left'

          assign slide_alt = block.settings.alt_text
          if slide_alt == blank
            assign slide_alt = 'sections.slider.image_alt' | t
          endif

          comment
            UM único link por slide (campo "Link"):
            - com "Texto do botão" preenchido -> vira CTA (o botão redireciona)
            - sem "Texto do botão"            -> a imagem inteira é o link
            Sem link não há redirecionamento; CTA sem link não é renderizado (botão sem ação é inútil).
          endcomment
          assign cta_text = block.settings.cta_label
          if cta_text == blank
            assign cta_text = 'general.see_more' | t
          endif
          assign show_cta = false
          if block.settings.cta_label != blank and block.settings.link != blank
            assign show_cta = true
          endif
          assign wrap_link = false
          if block.settings.link != blank and show_cta == false
            assign wrap_link = true
          endif

          assign has_overlay = false
          if block.settings.eyebrow != blank or block.settings.heading != blank or show_cta
            assign has_overlay = true
          endif

          assign pos_cls = 'items-start text-left'
          if content_position == 'center'
            assign pos_cls = 'items-center text-center'
          elsif content_position == 'right'
            assign pos_cls = 'items-end text-right'
          endif

          comment
            Scrim usa a COR DE FUNDO do color scheme (semi-transparente). Assim o texto
            (que usa a cor de TEXTO do scheme) sempre contrasta sobre qualquer foto,
            seja o scheme claro ou escuro. "Lateral" acompanha a posição do conteúdo;
            aceita 'side' (atual) e 'left' (legado).
          endcomment
          assign op = block.settings.scrim_opacity | default: 30 | divided_by: 100.0
          assign scrim_color = 'rgb(var(--color-background) / ' | append: op | append: ')'
          assign scrim_bg = 'none'
          case block.settings.scrim
            when 'side', 'left'
              if content_position == 'right'
                assign scrim_bg = 'linear-gradient(270deg, ' | append: scrim_color | append: ' 0%, transparent 65%)'
              elsif content_position == 'center'
                assign scrim_bg = scrim_color
              else
                assign scrim_bg = 'linear-gradient(90deg, ' | append: scrim_color | append: ' 0%, transparent 65%)'
              endif
            when 'bottom'
              assign scrim_bg = 'linear-gradient(0deg, ' | append: scrim_color | append: ' 0%, transparent 60%)'
            when 'full'
              assign scrim_bg = scrim_color
          endcase
        -%}

        {%- capture slide_picture -%}
          <picture>
            {% if block.settings.imgDesktop %}
              <source
                media="(min-width: 1024px)"
                srcset="{{ block.settings.imgDesktop | image_url }}"
                width="{{ block.settings.imgDesktopWidth | default: 1920 }}"
                height="{{ block.settings.imgDesktopHeight | default: 540 }}"
                class="w-full"
              >
            {% endif %}
            {% if block.settings.imgTablet %}
              <source
                media="(min-width: 500px)"
                srcset="{{ block.settings.imgTablet | image_url }}"
                width="{{ block.settings.imgTabletWidth | default: 1024 }}"
                height="{{ block.settings.imgTabletHeight | default: 540 }}"
                class="w-full"
              >
            {% endif %}
            <img
              src="{{ block.settings.imgMob | image_url }}"
              alt="{{ slide_alt | escape }}"
              width="{{ block.settings.imgMobWidth | default: 500 }}"
              height="{{ block.settings.imgMobHeight | default: 540 }}"
              class="w-full"
              loading="{% if is_first_slide %}eager{% else %}lazy{% endif %}"
              decoding="async"
              {% if is_first_slide %}fetchpriority="high"{% endif %}
            >
          </picture>
        {%- endcapture -%}

        {%- capture slide_overlay -%}
          {%- if has_overlay -%}
            {%- if scrim_bg != 'none' -%}
              <div class="absolute inset-0 pointer-events-none" style="background: {{ scrim_bg }};"></div>
            {%- endif -%}
            <div class="absolute inset-0 flex flex-col justify-center {{ pos_cls }} text-foreground px-6 md:px-14 lg:px-20">
              {%- if block.settings.eyebrow != blank -%}
                <p class="text-[11px] md:text-xs uppercase tracking-[0.3em] opacity-90 mb-3 md:mb-4">{{ block.settings.eyebrow }}</p>
              {%- endif -%}
              {%- if block.settings.heading != blank -%}
                <h2 class="font-light text-3xl md:text-5xl leading-tight max-w-xl mb-5 md:mb-7">{{ block.settings.heading }}</h2>
              {%- endif -%}
              {%- if show_cta -%}
                <a
                  href="{{ block.settings.link }}"
                  class="inline-flex items-center gap-2 bg-button text-button-text text-xs uppercase tracking-[0.16em] px-6 py-3.5 transition-opacity hover:opacity-90"
                >
                  {{ cta_text }}
                  {% render 'icon-arrow-right', size: 16 %}
                </a>
              {%- endif -%}
            </div>
          {%- endif -%}
        {%- endcapture -%}

        <div class="item relative">
          {%- if wrap_link -%}
            <a href="{{ block.settings.link }}" class="block">{{ slide_picture }}{{ slide_overlay }}</a>
          {%- else -%}
            {{ slide_picture }}{{ slide_overlay }}
          {%- endif -%}
        </div>
      {%- endfor -%}
    </div>
    {% if nav %}
      <div class="flex w-full items-center justify-center gap-5 absolute z-10 {% if dot %} bottom-10 {% else %} bottom-6 {% endif %}">
        <button
          onclick="sliderPrev('{{ section.id }}')"
          class="flex items-center justify-center w-9 h-9 rounded-full bg-background/60 backdrop-blur-sm text-foreground hover:bg-background/80 transition-colors"
          aria-label="{{ 'sections.slider.previous' | t }}"
        >
          {% render 'icon-angle-left', size: 20 %}
        </button>

        <div class="text-xs tracking-wide text-foreground rounded-full bg-background/60 backdrop-blur-sm py-1 px-3">
          <span class="index-active">1</span> / {{ section.blocks.size }}
        </div>

        <button
          onclick="sliderNext('{{ section.id }}')"
          class="flex items-center justify-center w-9 h-9 rounded-full bg-background/60 backdrop-blur-sm text-foreground hover:bg-background/80 transition-colors"
          aria-label="{{ 'sections.slider.next' | t }}"
        >
          {% render 'icon-angle-right', size: 20 %}
        </button>
      </div>
    {% endif %}
    </my-slider>
  </div>
</div>

{% schema %}
{
  "name": "t:sections.slider_image.name",
  "tag": "section",
  "class": "section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.slider_image.settings.color_scheme.label",
      "info": "t:sections.slider_image.settings.color_scheme.info",
      "default": "scheme-1"
    },
    {
      "type": "checkbox",
      "id": "fullwidth",
      "label": "t:sections.slider_image.settings.fullwidth.label",
      "info": "t:sections.slider_image.settings.fullwidth.info",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "loop",
      "label": "t:sections.slider_image.settings.loop.label",
      "info": "t:sections.slider_image.settings.loop.info",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "nav",
      "label": "t:sections.slider_image.settings.nav.label",
      "info": "t:sections.slider_image.settings.nav.info",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "dot",
      "label": "t:sections.slider_image.settings.dot.label",
      "info": "t:sections.slider_image.settings.dot.info",
      "default": true
    },
    {
      "type": "range",
      "min": 1,
      "max": 10,
      "step": 1,
      "label": "t:sections.slider_image.settings.qty_desk.label",
      "id": "qty-desk",
      "default": 3
    },
    {
      "type": "range",
      "min": 1,
      "max": 10,
      "step": 1,
      "label": "t:sections.slider_image.settings.qty_tablet.label",
      "id": "qty-tablet",
      "default": 2
    },
    {
      "type": "range",
      "min": 1,
      "max": 10,
      "step": 1,
      "label": "t:sections.slider_image.settings.qty_mob.label",
      "id": "qty-mob",
      "default": 1
    },
    {
      "type": "checkbox",
      "label": "t:sections.slider_image.settings.autoplay.label",
      "id": "autoplay",
      "info": "t:sections.slider_image.settings.autoplay.info",
      "default": true
    },
    {
      "type": "range",
      "id": "play-time",
      "max": 20,
      "min": 1,
      "step": 1,
      "label": "t:sections.slider_image.settings.play_time.label",
      "unit": "s",
      "default": 5
    },
    {
      "type": "checkbox",
      "id": "pause-hover",
      "label": "t:sections.slider_image.settings.pause_hover.label",
      "default": false
    }
  ],
  "blocks": [
    {
      "name": "t:sections.slider_image.blocks.image.name",
      "type": "test",
      "settings": [
        {
          "type": "url",
          "id": "link",
          "label": "t:sections.slider_image.blocks.image.settings.link.label",
          "info": "t:sections.slider_image.blocks.image.settings.link.info"
        },
        {
          "type": "image_picker",
          "id": "imgDesktop",
          "label": "t:sections.slider_image.blocks.image.settings.imgDesktop.label",
          "info": "t:sections.slider_image.blocks.image.settings.imgDesktop.info"
        },
        {
          "type": "number",
          "id": "imgDesktopWidth",
          "label": "t:sections.slider_image.blocks.image.settings.imgDesktopWidth.label"
        },
        {
          "type": "number",
          "id": "imgDesktopHeight",
          "label": "t:sections.slider_image.blocks.image.settings.imgDesktopHeight.label"
        },
        {
          "type": "image_picker",
          "id": "imgTablet",
          "label": "t:sections.slider_image.blocks.image.settings.imgTablet.label",
          "info": "t:sections.slider_image.blocks.image.settings.imgTablet.info"
        },
        {
          "type": "number",
          "id": "imgTabletWidth",
          "label": "t:sections.slider_image.blocks.image.settings.imgTabletWidth.label"
        },
        {
          "type": "number",
          "id": "imgTabletHeight",
          "label": "t:sections.slider_image.blocks.image.settings.imgTabletHeight.label"
        },
        {
          "type": "image_picker",
          "id": "imgMob",
          "label": "t:sections.slider_image.blocks.image.settings.imgMob.label",
          "info": "t:sections.slider_image.blocks.image.settings.imgMob.info"
        },
        {
          "type": "number",
          "id": "imgMobWidth",
          "label": "t:sections.slider_image.blocks.image.settings.imgMobWidth.label"
        },
        {
          "type": "number",
          "id": "imgMobHeight",
          "label": "t:sections.slider_image.blocks.image.settings.imgMobHeight.label"
        },
        {
          "type": "text",
          "id": "alt_text",
          "label": "t:sections.slider_image.blocks.image.settings.alt_text.label",
          "info": "t:sections.slider_image.blocks.image.settings.alt_text.info"
        },
        {
          "type": "header",
          "content": "t:sections.slider_image.blocks.image.settings.content_header.content"
        },
        {
          "type": "paragraph",
          "content": "t:sections.slider_image.blocks.image.settings.content_paragraph.content"
        },
        {
          "type": "text",
          "id": "eyebrow",
          "label": "t:sections.slider_image.blocks.image.settings.eyebrow.label"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "t:sections.slider_image.blocks.image.settings.heading.label"
        },
        {
          "type": "text",
          "id": "cta_label",
          "label": "t:sections.slider_image.blocks.image.settings.cta_label.label",
          "info": "t:sections.slider_image.blocks.image.settings.cta_label.info"
        },
        {
          "type": "select",
          "id": "content_position",
          "label": "t:sections.slider_image.blocks.image.settings.content_position.label",
          "default": "left",
          "options": [
            { "value": "left", "label": "t:sections.slider_image.blocks.image.settings.content_position.options.left" },
            { "value": "center", "label": "t:sections.slider_image.blocks.image.settings.content_position.options.center" },
            { "value": "right", "label": "t:sections.slider_image.blocks.image.settings.content_position.options.right" }
          ]
        },
        {
          "type": "select",
          "id": "scrim",
          "label": "t:sections.slider_image.blocks.image.settings.scrim.label",
          "info": "t:sections.slider_image.blocks.image.settings.scrim.info",
          "default": "side",
          "options": [
            { "value": "none", "label": "t:sections.slider_image.blocks.image.settings.scrim.options.none" },
            { "value": "side", "label": "t:sections.slider_image.blocks.image.settings.scrim.options.side" },
            { "value": "bottom", "label": "t:sections.slider_image.blocks.image.settings.scrim.options.bottom" },
            { "value": "full", "label": "t:sections.slider_image.blocks.image.settings.scrim.options.full" }
          ]
        },
        {
          "type": "range",
          "id": "scrim_opacity",
          "label": "t:sections.slider_image.blocks.image.settings.scrim_opacity.label",
          "min": 0,
          "max": 80,
          "step": 5,
          "unit": "%",
          "default": 30
        }
      ]
    }
  ],
  "max_blocks": 6,
  "presets": [
    {
      "name": "t:sections.slider_image.presets.name",
      "blocks": []
    }
  ]
}
{% endschema %}
