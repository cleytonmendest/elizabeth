## O que muda

<!-- Uma frase. O diff conta o resto. -->

Closes #

## Verificação

<!-- O CI roda tudo isto de novo; marque o que você já viu passar localmente. -->

- [ ] `npm run gate` verde (build + linters, sem violação nova)
- [ ] Testado no editor com um color scheme claro **e** um escuro
- [ ] Strings novas existem em `pt-BR` **e** `en.default`

## Baseline

<!-- Se você zerou dívida, rode `npm run lint:baseline` e diga quanto caiu.
     Se o número subiu, o CI vai reprovar: corrija a violação em vez de
     registrá-la. -->

Total: <!-- antes → depois -->

## Decisão estrutural?

<!-- Se este PR muda como algo funciona (não só o que faz), linke o ADR.
     Senão, "não". -->
